import { FastifyPluginAsync } from 'fastify';

export const graphRoutes: FastifyPluginAsync = async (fastify) => {
  // TODO: Implement graph creation routes
  fastify.log.info('Graph routes registered (stub)');
}; 