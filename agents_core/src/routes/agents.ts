import { FastifyPluginAsync } from "fastify";

export const agentRoutes: FastifyPluginAsync = async (fastify) => {
  // TODO: Implement agent management routes
  fastify.log.info("Agent routes registered (stub)");
};
